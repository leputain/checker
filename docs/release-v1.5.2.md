# Candidate Check v1.5.2

Патч-релиз исправляет accessibility-разметку карточки кандидата: внутри detail-dialog остаётся один однозначный заголовок с именем, а визуальное повторение в hero-блоке больше не объявляется вторым heading.

Проверка: WebKit E2E сценарий деталей кандидата должен находить единственный heading, связанный с dialog через `aria-labelledby`.
